export interface ICommonInputs {
    /**
     * Working directory
     */
    dir: string;
    /**
     * Repositories to work with directory
     */
    repositories: string[];
    /**
     * Be verbose
     */
    verbose: boolean;
}